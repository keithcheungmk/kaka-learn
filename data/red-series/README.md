# 紅輯書本 manifest

每本 JSON 係紅輯場景遊戲嘅內容交接資料。`cardsFile` 指向已核對嘅認字卡資料；`sourcePdf` 只作來源追溯，唔會由網站直接載入。

`contentStatus: cards-verified` 代表認字卡內容已可作學習／測驗資料；`sceneStatus: source-pending` 代表場景頁仍要由遊戲 lead 逐頁接入，未有資料就唔可以自行虛構句子或場景。
